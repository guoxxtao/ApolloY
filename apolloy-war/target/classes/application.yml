spring:
  datasource:
    url: jdbc:mysql://172.16.100.70:3306/apolloy_test?useSSL=false
    driverClassName: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    username: greatbear
    password: Sd@Im&e17LH


mybatis-plus:
    global-config:
      db-config:
        logic-delete-value: 1 # 逻辑已删除值(默认为 1)
        logic-not-delete-value: 0 # 逻辑未删除值(默认为 0)
        column-underline: true #驼峰下划线转换
    configuration:
      log-impl: org.apache.ibatis.logging.stdout.StdOutImpl #sql日志打印，线上不要开启


